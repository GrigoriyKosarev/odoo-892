<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.actions.server" id="bio_action_import_pricelist">
        <field name="name">Upload Pricelist</field>
        <field name="model_id" ref="product.model_product_pricelist"/>
        <field name="binding_model_id" ref="product.model_product_pricelist"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
            if records:
                for record in records:
                    action = record.action_bio_upload_pricelist()
                    if action:
                        action
        </field>
    </record>
</odoo>
